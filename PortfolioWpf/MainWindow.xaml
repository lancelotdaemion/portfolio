<Window x:Class="PortfolioWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PortfolioWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <!--xmlns:local="clr-namespace:ArticleSample">-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Ipsums}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding CurrentIpsum}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBox Grid.Column="1" Text="{Binding CurrentIpsum}"></TextBox>
    </Grid>
</Window>
