<Window x:Class="PortfolioWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PortfolioWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid Margin="25px">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="1.5*"></ColumnDefinition>
            <ColumnDefinition Width="85px"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Ipsums}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Name, Mode=OneWay}" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Border Grid.Column="1" Grid.Row="0" BorderThickness="1" BorderBrush="Black" Margin="15 0" Padding="5px">
                <TextBlock Text="{Binding CurrentIpsum, Mode=OneWay}" Height="20px"></TextBlock>
            </Border>
        </Grid>

        <Grid Grid.Column="2">
            <Border Grid.Column="1" Grid.Row="0" BorderThickness="0" BorderBrush="Black" Margin="0" Padding="5px">
                <Button Command="{Binding AddIpsumCommand}" Height="25px" VerticalAlignment="Top" Content="Add"/>
            </Border>
        </Grid>
    </Grid>
</Window>
